// 1. When we first start learning Processing, we usually work like this:
//    using fixed numbers to draw our shapes.

void setup() {
  size(420, 420);
  background(32);
  stroke(127);
  fill(#76E1E5);
  ellipse(210, 210, 400, 400);
}